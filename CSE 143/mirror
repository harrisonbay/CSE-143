public void mirror() {
    Stack<Integer> s = new Stack<Integer>();
    for (int i = 0; i < size; i++) {
        s.push(elementData[i]);
    }
    for(int i = size; i < size*2; i++) {
        elementData[i] = s.pop();
    }
    size *= 2;
}